{
    "buildCommand": "cd frontend && npm ci || npm i && npm run build",
    "outputDirectory": "frontend/build",
    "rewrites": [
        {"source": "/api/(.*)", "destination": "https://chatbot-production-80e5.up.railway.app/"}
    ]
}